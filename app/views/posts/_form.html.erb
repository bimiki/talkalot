<%= form_with(model: post) do |form| %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="field">
    <p>i forget what this field was so this is a marker</p>
  </div>


  <div class="actions">
    <%= form.submit "Save Post", class: "btn btn-success" %>
  </div>
<% end %>
